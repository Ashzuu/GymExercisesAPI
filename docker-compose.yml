services:
  # --- Service Base de Données ---
  postgres:
    image: postgres:16-alpine
    container_name: GymExercisesAPI_DB
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      # Persistance des données (pour ne pas perdre la DB au redémarrage)
      - postgres_data:/var/lib/postgresql/data
      # Script d'initialisation (exécuté uniquement au premier lancement)
      - ./db-init:/docker-entrypoint-initdb.d
    networks:
      - gym-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # --- Service API Quarkus ---
  api:
    build: .
    container_name: GymExercisesAPI
    ports:
      - "8080:8080"
    environment:
      # Configuration Quarkus dynamique
      QUARKUS_DATASOURCE_DB_KIND: postgresql
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://postgres:${DB_PORT}/${DB_NAME}
      QUARKUS_DATASOURCE_USERNAME: ${DB_USER}
      QUARKUS_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      # Hibernate valide juste le schéma (car tu as ton script SQL d'init)
      QUARKUS_HIBERNATE_ORM_DATABASE_GENERATION: validate
      # Log SQL pour le debug (optionnel, à retirer en prod)
      QUARKUS_HIBERNATE_ORM_LOG_SQL: "true"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - gym-network

volumes:
  postgres_data:

networks:
  gym-network:
    driver: bridge